- desc: Destroy environment
  device: localhost
  action_chain:
    - action: run_shell_commands
      action_vars:
        commands: 
          - deactivate
          - cd ..
          - "rm -rf repo-{prefix}"